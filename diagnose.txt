l-5.4.patchusfs4ksu/kernel_patches/50_add_susfs_in_kerne
========================================
1. SUSFS CMDLINE/BOOTCONFIG SOURCE
========================================
        }
        spin_unlock(&susfs_uname_spin_lock);
        SUSFS_LOGI("setting spoofed release: '%s', version: '%s'\n",
                                my_uname.release, my_uname.version);
        return 0;
}

void susfs_spoof_uname(struct new_utsname* tmp) {
        if (unlikely(my_uname.release[0] == '\0' || spin_is_locked(&susfs_uname_spin_lock)))
                return;
        strncpy(tmp->release, my_uname.release, __NEW_UTS_LEN);
        strncpy(tmp->version, my_uname.version, __NEW_UTS_LEN);
}
#endif // #ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME

/* set_log */
#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
void susfs_set_log(bool enabled) {
        spin_lock(&susfs_spin_lock);
        susfs_is_log_enabled = enabled;
        spin_unlock(&susfs_spin_lock);
        if (susfs_is_log_enabled) {
                pr_info("susfs: enable logging to kernel");
        } else {
                pr_info("susfs: disable logging to kernel");
        }
}
#endif // #ifdef CONFIG_KSU_SUSFS_ENABLE_LOG

/* spoof_cmdline_or_bootconfig */
#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
static char *fake_cmdline_or_bootconfig = NULL;
int susfs_set_cmdline_or_bootconfig(char* __user user_fake_cmdline_or_bootconfig) {
        int res;

        if (!fake_cmdline_or_bootconfig) {
                // 4096 is enough I guess
                fake_cmdline_or_bootconfig = kmalloc(SUSFS_FAKE_CMDLINE_OR_BOOTCONFIG_SIZE, GFP_KERNEL);
                if (!fake_cmdline_or_bootconfig) {
                        SUSFS_LOGE("no enough memory\n");
                        return -ENOMEM;
                }
        }

        spin_lock(&susfs_spin_lock);
        memset(fake_cmdline_or_bootconfig, 0, SUSFS_FAKE_CMDLINE_OR_BOOTCONFIG_SIZE);
        res = strncpy_from_user(fake_cmdline_or_bootconfig, user_fake_cmdline_or_bootconfig, SUSFS_FAKE_CMDLINE_OR_BOOTCONFIG_SIZE-1);
        spin_unlock(&susfs_spin_lock);

        if (res > 0) {
#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,1,0)
                SUSFS_LOGI("fake_cmdline_or_bootconfig is set, length of string: %lu\n", strlen(fake_cmdline_or_bootconfig));
#else
                SUSFS_LOGI("fake_cmdline_or_bootconfig is set, length of string: %u\n", strlen(fake_cmdline_or_bootconfig));
#endif
                return 0;
        }
        SUSFS_LOGI("failed setting fake_cmdline_or_bootconfig\n");
        return res;
}

int susfs_spoof_cmdline_or_bootconfig(struct seq_file *m) {
        if (fake_cmdline_or_bootconfig != NULL) {
                seq_puts(m, fake_cmdline_or_bootconfig);
                return 0;
        }
        return 1;
}
#endif

/* open_redirect */
#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
static DEFINE_HASHTABLE(OPEN_REDIRECT_HLIST, 10);
static int susfs_update_open_redirect_inode(struct st_susfs_open_redirect_hlist *new_entry) {
        struct path path_target;
        struct inode *inode_target;
        int err = 0;

        err = kern_path(new_entry->target_pathname, LOOKUP_FOLLOW, &path_target);
        if (err) {
                SUSFS_LOGE("Failed opening file '%s'\n", new_entry->target_pathname);
                return err;
        }

        inode_target = d_inode(path_target.dentry);
        if (!inode_target) {

========================================
2. SUSFS HEADER DECLARATIONS
========================================
#endif
/* try_umount */
#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
int susfs_add_try_umount(struct st_susfs_try_umount* __user user_info);
void susfs_try_umount(uid_t target_uid);
#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT
void susfs_auto_add_try_umount_for_bind_mount(struct path *path);
#endif // #ifdef CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT
#endif // #ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
/* spoof_uname */
#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
int susfs_set_uname(struct st_susfs_uname* __user user_info);
void susfs_spoof_uname(struct new_utsname* tmp);
#endif
/* set_log */
#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
void susfs_set_log(bool enabled);
#endif
/* spoof_cmdline_or_bootconfig */
#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
int susfs_set_cmdline_or_bootconfig(char* __user user_fake_boot_config);
int susfs_spoof_cmdline_or_bootconfig(struct seq_file *m);
#endif
/* open_redirect */
#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
int susfs_add_open_redirect(struct st_susfs_open_redirect* __user user_info);
struct filename* susfs_get_redirected_path(unsigned long ino);
#endif
/* sus_su */
#ifdef CONFIG_KSU_SUSFS_SUS_SU
int susfs_get_sus_su_working_mode(void);

========================================
3. KSU KCONFIG SPOOF SECTION
========================================
      - Allow spoofing the string returned by uname syscall to user-defined string.
      - Effective on all processes.

config KSU_SUSFS_ENABLE_LOG
    bool "Enable logging susfs log to kernel"
    depends on KSU_SUSFS
    default y
    help
      - Allow logging susfs log to kernel, uncheck it to completely disable all susfs log.

config KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
    bool "Enable to automatically hide ksu and susfs symbols from /proc/kallsyms"
    depends on KSU_SUSFS
    default y
    help
      - Automatically hide ksu and susfs symbols from '/proc/kallsyms'.
      - Effective on all processes.

config KSU_SUSFS_SPOOF_BOOTCONFIG
    bool "Enable to spoof /proc/cmdline"
    depends on KSU_SUSFS
    default y
    help
      - Spoof the output of /proc/cmdline with a user-defined file.
      - Effective on all processes.

config KSU_SUSFS_OPEN_REDIRECT
    bool "Enable to redirect a path to be opened with another path (experimental)"
    depends on KSU_SUSFS
    default y
    help
      - Allow redirecting a target path to be opened with another user-defined path.
      - Effective only on processes with uid < 2000.
      - Please be reminded that process with open access to the target and redirected path can be detected.

config KSU_SUSFS_SUS_SU

========================================
4. KSU CORE_HOOK SPOOF REFERENCES
========================================
                                return 0;
                        }
                        error = susfs_set_uname((struct st_susfs_uname __user*)arg3);
                        pr_info("susfs: CMD_SUSFS_SET_UNAME -> ret: %d\n", error);
                        if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
                                pr_info("susfs: copy_to_user() failed\n");
                        return 0;
                }
#endif //#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
                if (arg2 == CMD_SUSFS_ENABLE_LOG) {
                        int error = 0;
                        if (arg3 != 0 && arg3 != 1) {
                                pr_err("susfs: CMD_SUSFS_ENABLE_LOG -> arg3 can only be 0 or 1\n");
                                return 0;
                        }
                        susfs_set_log(arg3);
                        if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
                                pr_info("susfs: copy_to_user() failed\n");
                        return 0;
                }
#endif //#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
#ifdef CONFIG_KSU_SUSFS_SPOOF_BOOTCONFIG
                if (arg2 == CMD_SUSFS_SET_BOOTCONFIG) {
                        int error = 0;
                        if (!ksu_access_ok((void __user*)arg3, SUSFS_FAKE_BOOT_CONFIG_SIZE)) {
                                pr_err("susfs: CMD_SUSFS_SET_BOOTCONFIG -> arg3 is not accessible\n");
                                return 0;
                        }
                        if (!ksu_access_ok((void __user*)arg5, sizeof(error))) {
                                pr_err("susfs: CMD_SUSFS_SET_BOOTCONFIG -> arg5 is not accessible\n");
                                return 0;
                        }
                        error = susfs_set_bootconfig((char __user*)arg3);
                        pr_info("susfs: CMD_SUSFS_SET_BOOTCONFIG -> ret: %d\n", error);
                        if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
                                pr_info("susfs: copy_to_user() failed\n");
                        return 0;
                }
#endif //#ifdef CONFIG_KSU_SUSFS_SPOOF_BOOTCONFIG
#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
                if (arg2 == CMD_SUSFS_ADD_OPEN_REDIRECT) {
                        int error = 0;
                        if (!ksu_access_ok((void __user*)arg3, sizeof(struct st_susfs_open_redirect))) {
                                pr_err("susfs: CMD_SUSFS_ADD_OPEN_REDIRECT -> arg3 is not accessible\n");
                                return 0;
                        }
                        if (!ksu_access_ok((void __user*)arg5, sizeof(error))) {
                                pr_err("susfs: CMD_SUSFS_ADD_OPEN_REDIRECT -> arg5 is not accessible\n");
                                return 0;
                        }
                        error = susfs_add_open_redirect((struct st_susfs_open_redirect __user*)arg3);
                        pr_info("susfs: CMD_SUSFS_ADD_OPEN_REDIRECT -> ret: %d\n", error);
                        if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
                                pr_info("susfs: copy_to_user() failed\n");
                        return 0;
#ifdef CONFIG_KSU_SUSFS_SUS_OVERLAYFS
                        enabled_features |= (1 << 5);
#endif
#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
                        enabled_features |= (1 << 6);
#endif
#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT
                        enabled_features |= (1 << 7);
#endif
#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
                        enabled_features |= (1 << 8);
#endif
#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
                        enabled_features |= (1 << 9);
#endif
#ifdef CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
                        enabled_features |= (1 << 10);
#endif
#ifdef CONFIG_KSU_SUSFS_SPOOF_BOOTCONFIG
                        enabled_features |= (1 << 11);
#endif
#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
                        enabled_features |= (1 << 12);
#endif
#ifdef CONFIG_KSU_SUSFS_SUS_SU
                        enabled_features |= (1 << 13);
#endif
                        error = copy_to_user((void __user*)arg3, (void*)&enabled_features, sizeof(enabled_features));
                        pr_info("susfs: CMD_SUSFS_SHOW_ENABLED_FEATURES -> ret: %d\n", error);
                        if (copy_to_user((void __user*)arg5, &error, sizeof(error)))
                                pr_info("susfs: copy_to_user() failed\n");
                        return 0;
                }
                if (arg2 == CMD_SUSFS_SHOW_VARIANT) {
                        int error = 0;
                        int len_of_variant = strlen(SUSFS_VARIANT);

========================================
5. PATCH 50 CMDLINE/BOOTCONFIG SECTION
========================================
964-+#endif
965-+
966-    ovl_path_real(root_dentry, &path);
967-
968-    err = vfs_statfs(&path, buf);
969:diff --git a/fs/proc/bootconfig.c b/fs/proc/bootconfig.c
970-index 2e244ada1f97..792e1cb04b11 100644
971:--- a/fs/proc/bootconfig.c
972:+++ b/fs/proc/bootconfig.c
973-@@ -12,8 +12,19 @@
974-
975- static char *saved_boot_config;
976-
977:+#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
978-+extern int susfs_spoof_cmdline_or_bootconfig(struct seq_file *m);
979-+#endif
980-+
981- static int boot_config_proc_show(struct seq_file *m, void *v)
982- {
983:+#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
984-+   if (saved_boot_config) {
985-+           if (!susfs_spoof_cmdline_or_bootconfig(m)) {
986-+                   return 0;
987-+           }
988-+   }
989-+#endif
990-    if (saved_boot_config)
991-            seq_puts(m, saved_boot_config);
992-    return 0;
993-diff --git a/fs/proc/fd.c b/fs/proc/fd.c
994-index 6b634c0a9b6e..1bf6ca564248 100644
995---- a/fs/proc/fd.c
996-+++ b/fs/proc/fd.c
997-@@ -13,6 +13,9 @@
998- #include <linux/fs.h>
999-
1000- #include <linux/proc_fs.h>
1001-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1002-+#include <linux/susfs_def.h>
1003-+#endif
1004-
1005- #include "../mount.h"
1006- #include "internal.h"
1007-@@ -24,6 +27,9 @@ static int seq_show(struct seq_file *m, void *v)
1008-   int f_flags = 0, ret = -ENOENT;
1009-   struct file *file = NULL;
1010-   struct task_struct *task;
1011-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1012-+  struct mount *mnt = NULL;
1013-+#endif

========================================
6. PATCH 50 VFSMOUNT HUNK
========================================
422-+
423-+   if (res < 0)
424-+           return res;
425-+   mnt->mnt_id = res;
426-+   return 0;
427-+}
428-+#endif
429- static int mnt_alloc_id(struct mount *mnt)
430- {
431-    int res = ida_alloc(&mnt_id_ida, GFP_KERNEL);
432-@@ -114,6 +153,26 @@ static int mnt_alloc_id(struct mount *mnt)
433-
434- static void mnt_free_id(struct mount *mnt)
435- {
436-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
437:+   // We should first check the 'mnt->mnt.susfs_mnt_id_backup', see if it is DEFAULT_SUS_MNT_ID_FOR_KSU_PROC_UNSHARE
438-+   // if so, these mnt_id were not assigned by mnt_alloc_id() so we don't need to free it.
439:+   if (unlikely(mnt->mnt.susfs_mnt_id_backup == DEFAULT_SUS_MNT_ID_FOR_KSU_PROC_UNSHARE)) {
440-+           return;
441-+   }
442-+   // Now we can check if its mnt_id is sus
443-+   if (unlikely(mnt->mnt_id >= DEFAULT_SUS_MNT_ID)) {
444-+           ida_free(&susfs_mnt_id_ida, mnt->mnt_id);
445-+           return;
446-+   }
447:+   // Lastly if 'mnt->mnt.susfs_mnt_id_backup' is not 0, then it contains a backup origin mnt_id
448-+   // so we free it in the original way
449:+   if (likely(mnt->mnt.susfs_mnt_id_backup)) {
450:+           // If mnt->mnt.susfs_mnt_id_backup is not zero, it means mnt->mnt_id is spoofed,
451-+           // so here we return the original mnt_id for being freed.
452:+           ida_free(&mnt_id_ida, mnt->mnt.susfs_mnt_id_backup);
453-+           return;
454-+   }
455-+#endif
456-    ida_free(&mnt_id_ida, mnt->mnt_id);
457- }
458-
459-@@ -122,7 +181,20 @@ static void mnt_free_id(struct mount *mnt)
460-  */
461- static int mnt_alloc_group_id(struct mount *mnt)
462- {
463-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
464-+   int res;
465-+
466-+   // Check if mnt has sus mnt_id
467-+   if (mnt->mnt_id >= DEFAULT_SUS_MNT_ID) {
468-+           // If so, assign a sus mnt_group id DEFAULT_SUS_MNT_GROUP_ID from susfs_mnt_group_ida
469-+           res = ida_alloc_min(&susfs_mnt_group_ida, DEFAULT_SUS_MNT_GROUP_ID, GFP_KERNEL);
470-+           goto bypass_orig_flow;
471-+   }
472-+   res = ida_alloc_min(&mnt_group_ida, 1, GFP_KERNEL);
473-+bypass_orig_flow:
474-+#else
475-    int res = ida_alloc_min(&mnt_group_ida, 1, GFP_KERNEL);
476-+#endif
477-
478-    if (res < 0)
479-            return res;
480-@@ -135,6 +207,15 @@ static int mnt_alloc_group_id(struct mount *mnt)
481-  */
482- void mnt_release_group_id(struct mount *mnt)
--
513-+                           err = susfs_mnt_alloc_id(mnt);
514-+                   } else {
515-+                           mnt->mnt_id = custom_mnt_id;
516-+                           err = 0;
517-+                   }
518-+                   goto bypass_orig_flow;
519-+           }
520-+#endif
521-            err = mnt_alloc_id(mnt);
522-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
523-+bypass_orig_flow:
524-+#endif
525-            if (err)
526-                    goto out_free_cache;
527-
528:@@ -937,7 +1036,17 @@ struct vfsmount *vfs_create_mount(struct fs_context *fc)
529-    if (!fc->root)
530-            return ERR_PTR(-EINVAL);
531-
532-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
533-+   // For newly created mounts, the only caller process we care is KSU
534-+   if (unlikely(susfs_is_current_ksu_domain())) {
535-+           mnt = alloc_vfsmnt(fc->source ?: "none", true, 0);
536-+           goto bypass_orig_flow;
537-+   }
538-+   mnt = alloc_vfsmnt(fc->source ?: "none", false, 0);
539-+bypass_orig_flow:
540-+#else
541-    mnt = alloc_vfsmnt(fc->source ?: "none");
542-+#endif
543-    if (!mnt)
544-            return ERR_PTR(-ENOMEM);
545-
546:@@ -950,6 +1059,14 @@ struct vfsmount *vfs_create_mount(struct fs_context *fc)
547-    mnt->mnt_mountpoint     = mnt->mnt.mnt_root;
548-    mnt->mnt_parent         = mnt;
549-
550-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
551-+   // If caller process is zygote, then it is a normal mount, so we just reorder the mnt_id
552-+   if (susfs_is_current_zygote_domain()) {
553:+           mnt->mnt.susfs_mnt_id_backup = mnt->mnt_id;
554-+           mnt->mnt_id = current->susfs_last_fake_mnt_id++;
555-+   }
556-+#endif
557-+
558-    lock_mount_hash();
559-    list_add_tail(&mnt->mnt_instance, &mnt->mnt.mnt_sb->s_mounts);
560-    unlock_mount_hash();
561-@@ -1020,7 +1137,52 @@ static struct mount *clone_mnt(struct mount *old, struct dentry *root,
562-    struct mount *mnt;
563-    int err;
564-
565-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
566-+   bool is_current_ksu_domain = susfs_is_current_ksu_domain();
567-+   bool is_current_zygote_domain = susfs_is_current_zygote_domain();
568-+
569-+   /* - It is very important that we need to use CL_COPY_MNT_NS to identify whether
570-+    *   the clone is a copy_tree() or single mount like called by __do_loopback()
571-+    * - if caller process is KSU, consider the following situation:
572-+    *     1. it is NOT doing unshare => call alloc_vfsmnt() to assign a new sus mnt_id
573-+    *     2. it is doing unshare => spoof the new mnt_id with the old mnt_id
574-+    * - If caller process is zygote and old mnt_id is sus => call alloc_vfsmnt() to assign a new sus mnt_id
575-+    * - For the rest of caller process that doing unshare => call alloc_vfsmnt() to assign a new sus mnt_id only for old sus mount
576-+    */
577-+   // Firstly, check if it is KSU process
578-+   if (unlikely(is_current_ksu_domain)) {
579-+           // if it is doing single clone
580-+           if (!(flag & CL_COPY_MNT_NS)) {
581-+                   mnt = alloc_vfsmnt(old->mnt_devname, true, 0);
582-+                   goto bypass_orig_flow;
583-+           }
584-+           // if it is doing unshare
585-+           mnt = alloc_vfsmnt(old->mnt_devname, true, old->mnt_id);
586-+           if (mnt) {
587:+                   mnt->mnt.susfs_mnt_id_backup = DEFAULT_SUS_MNT_ID_FOR_KSU_PROC_UNSHARE;
588-+           }
589-+           goto bypass_orig_flow;
590-+   }
591-+   // Secondly, check if it is zygote process and no matter it is doing unshare or not
592-+   if (likely(is_current_zygote_domain) && (old->mnt_id >= DEFAULT_SUS_MNT_ID)) {
593-+           /* Important Note:
594-+            *  - Here we can't determine whether the unshare is called zygisk or not,
595-+            *    so we can only patch out the unshare code in zygisk source code for now
596-+            *  - But at least we can deal with old sus mounts using alloc_vfsmnt()
597-+            */
598-+           mnt = alloc_vfsmnt(old->mnt_devname, true, 0);
599-+           goto bypass_orig_flow;
600-+   }
601-+   // Lastly, for other process that is doing unshare operation, but only deal with old sus mount
602-+   if ((flag & CL_COPY_MNT_NS) && (old->mnt_id >= DEFAULT_SUS_MNT_ID)) {
603-+           mnt = alloc_vfsmnt(old->mnt_devname, true, 0);
604-+           goto bypass_orig_flow;
605-+   }
606-+   mnt = alloc_vfsmnt(old->mnt_devname, false, 0);
607-+bypass_orig_flow:
608-+#else
609-    mnt = alloc_vfsmnt(old->mnt_devname);
610-+#endif
611-    if (!mnt)
612-            return ERR_PTR(-ENOMEM);
613-
614-@@ -1043,6 +1205,15 @@ static struct mount *clone_mnt(struct mount *old, struct dentry *root,
615-    mnt->mnt.mnt_root = dget(root);
616-    mnt->mnt_mountpoint = mnt->mnt.mnt_root;
617-    mnt->mnt_parent = mnt;
618-+
619-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
620-+   // If caller process is zygote and not doing unshare, so we just reorder the mnt_id
621-+   if (likely(is_current_zygote_domain) && !(flag & CL_ZYGOTE_COPY_MNT_NS)) {
622:+           mnt->mnt.susfs_mnt_id_backup = mnt->mnt_id;
623-+           mnt->mnt_id = current->susfs_last_fake_mnt_id++;
624-+   }
625-+#endif
626-+
627-    lock_mount_hash();
628-    list_add_tail(&mnt->mnt_instance, &sb->s_mounts);
629-    unlock_mount_hash();
630-@@ -2331,6 +2502,27 @@ static int do_loopback(struct path *path, const char *old_name,
631-            umount_tree(mnt, UMOUNT_SYNC);
632-            unlock_mount_hash();
633-    }
634-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT) || defined(CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT)
635-+   // Check if bind mounted path should be hidden and umounted automatically.
636-+   // And we target only process with ksu domain.
637-+   if (susfs_is_current_ksu_domain()) {
638-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT)
639-+           if (susfs_is_auto_add_sus_bind_mount_enabled &&
640-+                           susfs_auto_add_sus_bind_mount(old_name, &old_path)) {
641-+                   goto orig_flow;
642-+           }
643-+#endif
644-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT)
645-+           if (susfs_is_auto_add_try_umount_for_bind_mount_enabled) {
646-+                   susfs_auto_add_try_umount_for_bind_mount(path);
647-+           }
648-+#endif
649-+   }
650-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT)
651-+orig_flow:
652-+#endif
--
689-+   if (is_zygote_pid) {
690-+           // Let clone_mnt() in copy_tree() know copy_mnt_ns() is run by zygote process
691-+           copy_flags |= CL_ZYGOTE_COPY_MNT_NS;
692-+   }
693-+#endif
694-+
695-    new = copy_tree(old, old->mnt.mnt_root, copy_flags);
696-    if (IS_ERR(new)) {
697-            namespace_unlock();
698-@@ -3299,6 +3510,29 @@ struct mnt_namespace *copy_mnt_ns(unsigned long flags, struct mnt_namespace *ns,
699-            while (p->mnt.mnt_root != q->mnt.mnt_root)
700-                    p = next_mnt(p, old);
701-    }
702-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
703-+   // current->susfs_last_fake_mnt_id -> to record last valid fake mnt_id to zygote pid
704:+   // q->mnt.susfs_mnt_id_backup -> original mnt_id
705-+   // q->mnt_id -> will be modified to the fake mnt_id
706-+
707-+   // Here We are only interested in processes of which original mnt namespace belongs to zygote
708-+   // Also we just make use of existing 'q' mount pointer, no need to delcare extra mount pointer
709-+   if (is_zygote_pid) {
710-+           last_entry_mnt_id = list_first_entry(&new_ns->list, struct mount, mnt_list)->mnt_id;
711-+           list_for_each_entry(q, &new_ns->list, mnt_list) {
712-+                   if (unlikely(q->mnt_id >= DEFAULT_SUS_MNT_ID)) {
713-+                           continue;
714-+                   }
715:+                   q->mnt.susfs_mnt_id_backup = q->mnt_id;
716-+                   q->mnt_id = last_entry_mnt_id++;
717-+           }
718-+   }
719-+   // Assign the 'last_entry_mnt_id' to 'current->susfs_last_fake_mnt_id' for later use.
720-+   // should be fine here assuming zygote is forking/unsharing app in one single thread.
721-+   // Or should we put a lock here?
722-+   current->susfs_last_fake_mnt_id = last_entry_mnt_id;
723-+#endif
724-+
725-    namespace_unlock();
726-
727-    if (rootmnt)
728-@@ -3581,6 +3815,13 @@ SYSCALL_DEFINE5(move_mount,
729-    path_put(&to_path);
730- out_from:
731-    path_put(&from_path);
732-+#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT
733-+   // For Legacy KSU mount scheme
734-+   if (!ret && susfs_is_auto_add_sus_ksu_default_mount_enabled && susfs_is_current_ksu_domain()) {
735-+           susfs_auto_add_sus_ksu_default_mount(to_pathname);
736-+   }
737-+#endif
738-+
739-    return ret;
740- }
741-
742-@@ -4037,3 +4278,37 @@ const struct proc_ns_operations mntns_operations = {
743-    .install        = mntns_install,
744-    .owner          = mntns_owner,
745- };
--
1108-
1109-diff --git a/fs/proc_namespace.c b/fs/proc_namespace.c
1110-index 273ee82d8aa9..86bd6d140962 100644
1111---- a/fs/proc_namespace.c
1112-+++ b/fs/proc_namespace.c
1113-@@ -12,6 +12,9 @@
1114- #include <linux/security.h>
1115- #include <linux/fs_struct.h>
1116- #include <linux/sched/task.h>
1117-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1118-+#include <linux/susfs_def.h>
1119-+#endif
1120-
1121- #include "proc/internal.h" /* only for get_proc_task() in ->open() */
1122-
1123:@@ -102,6 +105,11 @@ static int show_vfsmnt(struct seq_file *m, struct vfsmount *mnt)
1124-   struct super_block *sb = mnt_path.dentry->d_sb;
1125-   int err;
1126-
1127-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1128-+  if (unlikely(r->mnt_id >= DEFAULT_SUS_MNT_ID))
1129-+          return 0;
1130-+#endif
1131-+
1132-   if (sb->s_op->show_devname) {
1133-           err = sb->s_op->show_devname(m, mnt_path.dentry);
1134-           if (err)
1135:@@ -136,6 +144,11 @@ static int show_mountinfo(struct seq_file *m, struct vfsmount *mnt)
1136-   struct path mnt_path = { .dentry = mnt->mnt_root, .mnt = mnt };
1137-   int err;
1138-
1139-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1140-+  if (unlikely(r->mnt_id >= DEFAULT_SUS_MNT_ID))
1141-+          return 0;
1142-+#endif
1143-+
1144-   seq_printf(m, "%i %i %u:%u ", r->mnt_id, r->mnt_parent->mnt_id,
1145-              MAJOR(sb->s_dev), MINOR(sb->s_dev));
1146-   if (sb->s_op->show_path) {
1147:@@ -198,6 +211,11 @@ static int show_vfsstat(struct seq_file *m, struct vfsmount *mnt)
1148-   struct super_block *sb = mnt_path.dentry->d_sb;
1149-   int err;
1150-
1151-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1152-+  if (unlikely(r->mnt_id >= DEFAULT_SUS_MNT_ID))
1153-+          return 0;
1154-+#endif
1155-+
1156-   /* device */
1157-   if (sb->s_op->show_devname) {
1158-           seq_puts(m, "device ");
1159-diff --git a/fs/readdir.c b/fs/readdir.c
1160-index 07a3b5baa404..29b8e886819c 100644
1161---- a/fs/readdir.c
1162-+++ b/fs/readdir.c
1163-@@ -21,6 +21,9 @@
1164- #include <linux/unistd.h>
1165- #include <linux/compat.h>
1166- #include <linux/uaccess.h>
1167-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
1168-+#include <linux/susfs_def.h>
1169-+#endif
1170-
1171- #include <asm/unaligned.h>
1172-
1173-@@ -36,6 +39,9 @@
1174-   unsafe_copy_to_user(dst, src, len, label);     \
1175- } while (0)
1176-
1177-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
--
1373-   if (!s)
1374-           return -EINVAL;
1375-
1376-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1377-+  if (unlikely(s->s_root->d_inode->i_state & INODE_STATE_SUS_MOUNT)) {
1378-+          return -EINVAL;
1379-+  }
1380-+#endif
1381-   err = statfs_by_dentry(s->s_root, sbuf);
1382-   drop_super(s);
1383-   return err;
1384-diff --git a/include/linux/mount.h b/include/linux/mount.h
1385-index ca388cc44686..66f19067cf93 100644
1386---- a/include/linux/mount.h
1387-+++ b/include/linux/mount.h
1388:@@ -73,7 +73,11 @@ struct vfsmount {
1389-   ANDROID_KABI_RESERVE(1);
1390-   ANDROID_KABI_RESERVE(2);
1391-   ANDROID_KABI_RESERVE(3);
1392-+#ifdef CONFIG_KSU_SUSFS
1393:+  ANDROID_KABI_USE(4, u64 susfs_mnt_id_backup);
1394-+#else
1395-   ANDROID_KABI_RESERVE(4);
1396-+#endif
1397- } __randomize_layout;
1398-
1399- struct file; /* forward dec */
1400-diff --git a/include/linux/sched.h b/include/linux/sched.h
1401-index 3b6cd312986b..1e5320dc601c 100644
1402---- a/include/linux/sched.h
1403-+++ b/include/linux/sched.h
1404-@@ -1307,8 +1307,16 @@ struct task_struct {
1405-   ANDROID_KABI_RESERVE(4);
1406-   ANDROID_KABI_RESERVE(5);
1407-   ANDROID_KABI_RESERVE(6);
1408-+#ifdef CONFIG_KSU_SUSFS
1409-+  ANDROID_KABI_USE(7, u64 susfs_task_state);
1410-+#else
1411-   ANDROID_KABI_RESERVE(7);
1412-+#endif
1413-+#ifdef CONFIG_KSU_SUSFS
1414-+  ANDROID_KABI_USE(8, u64 susfs_last_fake_mnt_id);
1415-+#else
1416-   ANDROID_KABI_RESERVE(8);
1417-+#endif
1418-
1419-   /*
1420-    * New fields for task_struct should be added above here, so that
1421-diff --git a/kernel/kallsyms.c b/kernel/kallsyms.c
1422-index 14de0c7bf756..a4c563980af4 100644
1423---- a/kernel/kallsyms.c

========================================
7. CURRENT SCHED.H SUSFS TARGET
========================================
1471-   unsigned long                   lowest_stack;
1472-   unsigned long                   prev_lowest_stack;
1473-#endif
1474-
1475-   ANDROID_VENDOR_DATA_ARRAY(1, 3);
1476-
1477-   ANDROID_KABI_RESERVE(1);
1478-   ANDROID_KABI_RESERVE(2);
1479-   ANDROID_KABI_RESERVE(3);
1480-   ANDROID_KABI_RESERVE(4);
1481-   ANDROID_KABI_RESERVE(5);
1482-   ANDROID_KABI_RESERVE(6);
1483:   ANDROID_KABI_RESERVE(7);
1484-   ANDROID_KABI_RESERVE(8);
1485-
1486-   /*
1487-    * New fields for task_struct should be added above here, so that
1488-    * they are included in the randomized portion of task_struct.
1489-    */
1490-   randomized_struct_fields_end
1491-
1492-   /* CPU-specific state of this task: */
1493-   struct thread_struct            thread;
1494-
1495-   /*
1496-    * WARNING: on x86, 'thread_struct' contains a variable-sized
1497-    * structure.  It *MUST* be at the end of 'task_struct'.
1498-    *
1499-    * Do not put anything below here!
1500-    */
1501-};
1502-
1503-static inline struct pid *task_pid(struct task_struct *task)
1504-{
1505-   return task->thread_pid;
1506-}
1507-
1508-/*

========================================
8. PATCH 50 SCHED.H HUNK
========================================
18-@@ -32,6 +32,9 @@
19- #include <linux/bit_spinlock.h>
20- #include <linux/rculist_bl.h>
21- #include <linux/list_lru.h>
22-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
23-+#include <linux/susfs_def.h>
24-+#endif
25- #include "internal.h"
26- #include "mount.h"
27-
28-@@ -2295,6 +2298,11 @@ struct dentry *__d_lookup_rcu(const struct dentry *parent,
29-                             continue;
30-                     if (dentry_cmp(dentry, str, hashlen_len(hashlen)) != 0)
31-                             continue;
32-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
33:+                    if (dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
34-+                            continue;
35-+                    }
36-+#endif
37-             }
38-             *seqp = seq;
39-             return dentry;
40-@@ -2378,6 +2386,12 @@ struct dentry *__d_lookup(const struct dentry *parent, const struct qstr *name)
41-             if (dentry->d_name.hash != hash)
42-                     continue;
43-
44-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
45:+            if (dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
46-+                    continue;
47-+            }
48-+#endif
49-+
50-             spin_lock(&dentry->d_lock);
51-             if (dentry->d_parent != parent)
52-                     goto next;
53-diff --git a/fs/devpts/inode.c b/fs/devpts/inode.c
54-index 4f25015aa534..2938aa2a808e 100644
55---- a/fs/devpts/inode.c
56-+++ b/fs/devpts/inode.c
57-@@ -596,6 +596,11 @@ struct dentry *devpts_pty_new(struct pts_fs_info *fsi, int index, void *priv)
58-     return dentry;
59- }
60-
61-+#ifdef CONFIG_KSU_SUSFS_SUS_SU
62-+extern bool ksu_devpts_hook;
63-+extern int ksu_handle_devpts(struct inode*);
64-+#endif
65-+
66- /**
67-  * devpts_get_priv -- get private data for a slave
68-  * @pts_inode: inode of the slave
69-@@ -604,6 +609,12 @@ struct dentry *devpts_pty_new(struct pts_fs_info *fsi, int index, void *priv)
70-  */
71- void *devpts_get_priv(struct dentry *dentry)
72- {
73-+#ifdef CONFIG_KSU_SUSFS_SUS_SU
74-+    if (likely(ksu_devpts_hook)) {
75-+            ksu_handle_devpts(dentry->d_inode);
76-+    }
77-+#endif
78-+
79-     if (dentry->d_sb->s_magic != DEVPTS_SUPER_MAGIC)
80-             return NULL;
--
113- #include <linux/bitops.h>
114- #include <linux/init_task.h>
115- #include <linux/uaccess.h>
116-+#if defined(CONFIG_KSU_SUSFS_SUS_PATH) || defined(CONFIG_KSU_SUSFS_OPEN_REDIRECT)
117-+#include <linux/susfs_def.h>
118-+#endif
119-+
120-
121- #include "internal.h"
122- #include "mount.h"
123-@@ -987,6 +991,12 @@ static inline int may_follow_link(struct nameidata *nd)
124-    const struct inode *parent;
125-    kuid_t puid;
126-
127-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
128:+   if (nd->inode && unlikely(nd->inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
129-+           return -ENOENT;
130-+   }
131-+#endif
132-+
133-    if (!sysctl_protected_symlinks)
134-            return 0;
135-
136-@@ -1064,6 +1074,12 @@ static int may_linkat(struct path *link)
137- {
138-    struct inode *inode = link->dentry->d_inode;
139-
140-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
141:+   if (inode && unlikely(inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
142-+           return -ENOENT;
143-+   }
144-+#endif
145-+
146-    /* Inode writeback is not safe when the uid or gid are invalid. */
147-    if (!uid_valid(inode->i_uid) || !gid_valid(inode->i_gid))
148-            return -EOVERFLOW;
149-@@ -1105,6 +1121,12 @@ static int may_linkat(struct path *link)
150- static int may_create_in_sticky(umode_t dir_mode, kuid_t dir_uid,
151-                            struct inode * const inode)
152- {
153-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
154:+   if (unlikely(inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
155-+           return -ENOENT;
156-+   }
157-+#endif
158-+
159-    if ((!sysctl_protected_fifos && S_ISFIFO(inode->i_mode)) ||
160-        (!sysctl_protected_regular && S_ISREG(inode->i_mode)) ||
161-        likely(!(dir_mode & S_ISVTX)) ||
162-@@ -1625,6 +1647,12 @@ static struct dentry *__lookup_hash(const struct qstr *name,
163-            dput(dentry);
164-            dentry = old;
165-    }
166-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
167:+   if (!IS_ERR(dentry) && dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
168-+           dput(dentry);
169-+           return ERR_PTR(-ENOENT);
170-+   }
171-+#endif
172-    return dentry;
173- }
174-
175-@@ -1752,6 +1780,12 @@ static struct dentry *__lookup_slow(const struct qstr *name,
176-                    dentry = old;
177-            }
178-    }
179-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
180:+   if (!IS_ERR(dentry) && dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
181-+           dput(dentry);
182-+           return ERR_PTR(-ENOENT);
183-+   }
184-+#endif
185-    return dentry;
186- }
187-
188-@@ -2240,6 +2274,12 @@ static int link_path_walk(const char *name, struct nameidata *nd)
189-                    }
190-                    return -ENOTDIR;
191-            }
192-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
193-+           // we deal with sus sub path here
194:+           if (nd->inode && unlikely(nd->inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
195-+                   return 0;
196-+           }
197-+#endif
198-    }
199- }
200-
201-@@ -2429,6 +2469,12 @@ int filename_lookup(int dfd, struct filename *name, unsigned flags,
202-    if (likely(!retval))
203-            audit_inode(name, path->dentry, 0);
204-    restore_nameidata();
205-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
206:+   if (!retval && path->dentry->d_inode && unlikely(path->dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
207-+           putname(name);
208-+           return -ENOENT;
209-+   }
210-+#endif
211-    putname(name);
212-    return retval;
213- }
214-@@ -2905,6 +2951,12 @@ static int may_delete(struct inode *dir, struct dentry *victim, bool isdir)
215-    if (IS_APPEND(dir))
216-            return -EPERM;
217-
218-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
219:+   if (unlikely(inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
220-+           return -ENOENT;
221-+   }
222-+#endif
223-+
224-    if (check_sticky(dir, inode) || IS_APPEND(inode) ||
225-        IS_IMMUTABLE(inode) || IS_SWAPFILE(inode) || HAS_UNMAPPED_ID(inode))
226-            return -EPERM;
227-@@ -2933,8 +2985,20 @@ static int may_delete(struct inode *dir, struct dentry *victim, bool isdir)
228-  */
229- static inline int may_create(struct inode *dir, struct dentry *child)
230- {
231-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
232-+   int error;
233-+#endif
234-    struct user_namespace *s_user_ns;
235-    audit_inode_child(dir, child, AUDIT_TYPE_CHILD_CREATE);
236-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
237:+   if (child->d_inode && unlikely(child->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
238-+           error = inode_permission(dir, MAY_WRITE | MAY_EXEC);
239-+           if (error) {
240-+                   return error;
241-+           }
242-+           return -ENOENT;
243-+   }
244-+#endif
245-    if (child->d_inode)
246-            return -EEXIST;
247-    if (IS_DEADDIR(dir))
248-@@ -3098,6 +3162,12 @@ static int may_open(const struct path *path, int acc_mode, int flag)
249-    if (!inode)
250-            return -ENOENT;
251-
252-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
253:+   if (unlikely(inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
254-+           return -ENOENT;
255-+   }
256-+#endif
257-+
258-    switch (inode->i_mode & S_IFMT) {
259-    case S_IFLNK:
260-            return -ELOOP;
261-@@ -3169,7 +3239,20 @@ static inline int open_to_namei_flags(int flag)
262- static int may_o_create(const struct path *dir, struct dentry *dentry, umode_t mode)
263- {
264-    struct user_namespace *s_user_ns;
265-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
266-+   int error;
267-+
268:+   if (dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
269-+           error = inode_permission(dir->dentry->d_inode, MAY_WRITE | MAY_EXEC);
270-+           if (error) {
271-+                   return error;
272-+           }
273-+           return -ENOENT;
274-+   }
275-+   error = security_path_mknod(dir, dentry, mode, 0);
276-+#else
277-    int error = security_path_mknod(dir, dentry, mode, 0);
278-+#endif
279-    if (error)
280-            return error;
281-
282-@@ -3308,6 +3391,12 @@ static int lookup_open(struct nameidata *nd, struct path *path,
283-    }
284-    if (dentry->d_inode) {
285-            /* Cached positive dentry: will open in f_op->open */
286-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
287:+           if (unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
288-+                   dput(dentry);
289-+                   return -ENOENT;
290-+           }
291-+#endif
292-            goto out_no_open;
293-    }
294-
295-@@ -3350,6 +3439,16 @@ static int lookup_open(struct nameidata *nd, struct path *path,
296-                                mode);
297-            if (unlikely(error == -ENOENT) && create_error)
298-                    error = create_error;
299-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
300:+           if (!IS_ERR(dentry) && dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
301-+                   if (create_error) {
302-+                           dput(dentry);
303-+                           return create_error;
304-+                   }
305-+                   dput(dentry);
306-+                   return -ENOENT;
307-+           }
308-+#endif
309-            return error;
310-    }
311-
312-@@ -3365,6 +3464,12 @@ static int lookup_open(struct nameidata *nd, struct path *path,
313-                    }
314-                    dput(dentry);
315-                    dentry = res;
316-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
317:+                   if (dentry->d_inode && unlikely(dentry->d_inode->i_state & INODE_STATE_SUS_PATH) && likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
318-+                           dput(dentry);
319-+                           return -ENOENT;
320-+                   }
321-+#endif
322-            }
323-    }
324-
325-@@ -3702,12 +3807,19 @@ static struct file *path_openat(struct nameidata *nd,
326-    return ERR_PTR(error);
327- }
328-
329-+#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
330-+extern struct filename* susfs_get_redirected_path(unsigned long ino);
331-+#endif
332-+
333- struct file *do_filp_open(int dfd, struct filename *pathname,
334-            const struct open_flags *op)
335- {
336-    struct nameidata nd;
337-    int flags = op->lookup_flags;
338-    struct file *filp;
339-+#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
340-+   struct filename *fake_pathname;
341-+#endif
342-
343-    set_nameidata(&nd, dfd, pathname);
344-    filp = path_openat(&nd, op, flags | LOOKUP_RCU);
345-@@ -3715,6 +3827,25 @@ struct file *do_filp_open(int dfd, struct filename *pathname,
346-            filp = path_openat(&nd, op, flags);
347-    if (unlikely(filp == ERR_PTR(-ESTALE)))
348-            filp = path_openat(&nd, op, flags | LOOKUP_REVAL);
349-+#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
350-+   if (!IS_ERR(filp) && unlikely(filp->f_inode->i_state & INODE_STATE_OPEN_REDIRECT) && current_uid().val < 2000) {
351-+           fake_pathname = susfs_get_redirected_path(filp->f_inode->i_ino);
352-+           if (!IS_ERR(fake_pathname)) {
--
539-+bypass_orig_flow:
540-+#else
541-    mnt = alloc_vfsmnt(fc->source ?: "none");
542-+#endif
543-    if (!mnt)
544-            return ERR_PTR(-ENOMEM);
545-
546-@@ -950,6 +1059,14 @@ struct vfsmount *vfs_create_mount(struct fs_context *fc)
547-    mnt->mnt_mountpoint     = mnt->mnt.mnt_root;
548-    mnt->mnt_parent         = mnt;
549-
550-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
551-+   // If caller process is zygote, then it is a normal mount, so we just reorder the mnt_id
552-+   if (susfs_is_current_zygote_domain()) {
553-+           mnt->mnt.susfs_mnt_id_backup = mnt->mnt_id;
554:+           mnt->mnt_id = current->susfs_last_fake_mnt_id++;
555-+   }
556-+#endif
557-+
558-    lock_mount_hash();
559-    list_add_tail(&mnt->mnt_instance, &mnt->mnt.mnt_sb->s_mounts);
560-    unlock_mount_hash();
561-@@ -1020,7 +1137,52 @@ static struct mount *clone_mnt(struct mount *old, struct dentry *root,
562-    struct mount *mnt;
563-    int err;
564-
565-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
566-+   bool is_current_ksu_domain = susfs_is_current_ksu_domain();
567-+   bool is_current_zygote_domain = susfs_is_current_zygote_domain();
568-+
569-+   /* - It is very important that we need to use CL_COPY_MNT_NS to identify whether
570-+    *   the clone is a copy_tree() or single mount like called by __do_loopback()
571-+    * - if caller process is KSU, consider the following situation:
572-+    *     1. it is NOT doing unshare => call alloc_vfsmnt() to assign a new sus mnt_id
573-+    *     2. it is doing unshare => spoof the new mnt_id with the old mnt_id
574-+    * - If caller process is zygote and old mnt_id is sus => call alloc_vfsmnt() to assign a new sus mnt_id
575-+    * - For the rest of caller process that doing unshare => call alloc_vfsmnt() to assign a new sus mnt_id only for old sus mount
576-+    */
577-+   // Firstly, check if it is KSU process
578-+   if (unlikely(is_current_ksu_domain)) {
579-+           // if it is doing single clone
580-+           if (!(flag & CL_COPY_MNT_NS)) {
581-+                   mnt = alloc_vfsmnt(old->mnt_devname, true, 0);
582-+                   goto bypass_orig_flow;
583-+           }
584-+           // if it is doing unshare
585-+           mnt = alloc_vfsmnt(old->mnt_devname, true, old->mnt_id);
586-+           if (mnt) {
587-+                   mnt->mnt.susfs_mnt_id_backup = DEFAULT_SUS_MNT_ID_FOR_KSU_PROC_UNSHARE;
588-+           }
589-+           goto bypass_orig_flow;
--
608-+#else
609-    mnt = alloc_vfsmnt(old->mnt_devname);
610-+#endif
611-    if (!mnt)
612-            return ERR_PTR(-ENOMEM);
613-
614-@@ -1043,6 +1205,15 @@ static struct mount *clone_mnt(struct mount *old, struct dentry *root,
615-    mnt->mnt.mnt_root = dget(root);
616-    mnt->mnt_mountpoint = mnt->mnt.mnt_root;
617-    mnt->mnt_parent = mnt;
618-+
619-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
620-+   // If caller process is zygote and not doing unshare, so we just reorder the mnt_id
621-+   if (likely(is_current_zygote_domain) && !(flag & CL_ZYGOTE_COPY_MNT_NS)) {
622-+           mnt->mnt.susfs_mnt_id_backup = mnt->mnt_id;
623:+           mnt->mnt_id = current->susfs_last_fake_mnt_id++;
624-+   }
625-+#endif
626-+
627-    lock_mount_hash();
628-    list_add_tail(&mnt->mnt_instance, &sb->s_mounts);
629-    unlock_mount_hash();
630-@@ -2331,6 +2502,27 @@ static int do_loopback(struct path *path, const char *old_name,
631-            umount_tree(mnt, UMOUNT_SYNC);
632-            unlock_mount_hash();
633-    }
634-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT) || defined(CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT)
635-+   // Check if bind mounted path should be hidden and umounted automatically.
636-+   // And we target only process with ksu domain.
637-+   if (susfs_is_current_ksu_domain()) {
638-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT)
639-+           if (susfs_is_auto_add_sus_bind_mount_enabled &&
640-+                           susfs_auto_add_sus_bind_mount(old_name, &old_path)) {
641-+                   goto orig_flow;
642-+           }
643-+#endif
644-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT)
645-+           if (susfs_is_auto_add_try_umount_for_bind_mount_enabled) {
646-+                   susfs_auto_add_try_umount_for_bind_mount(path);
647-+           }
648-+#endif
649-+   }
650-+#if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT)
651-+orig_flow:
652-+#endif
653-+#endif // #if defined(CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT) || defined(CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT)
654-+
655- out2:
656-    unlock_mount(mp);
657- out:
658-@@ -3163,6 +3355,12 @@ long do_mount(const char *dev_name, const char __user *dir_name,
--
688-+   copy_flags |= CL_COPY_MNT_NS;
689-+   if (is_zygote_pid) {
690-+           // Let clone_mnt() in copy_tree() know copy_mnt_ns() is run by zygote process
691-+           copy_flags |= CL_ZYGOTE_COPY_MNT_NS;
692-+   }
693-+#endif
694-+
695-    new = copy_tree(old, old->mnt.mnt_root, copy_flags);
696-    if (IS_ERR(new)) {
697-            namespace_unlock();
698-@@ -3299,6 +3510,29 @@ struct mnt_namespace *copy_mnt_ns(unsigned long flags, struct mnt_namespace *ns,
699-            while (p->mnt.mnt_root != q->mnt.mnt_root)
700-                    p = next_mnt(p, old);
701-    }
702-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
703:+   // current->susfs_last_fake_mnt_id -> to record last valid fake mnt_id to zygote pid
704-+   // q->mnt.susfs_mnt_id_backup -> original mnt_id
705-+   // q->mnt_id -> will be modified to the fake mnt_id
706-+
707-+   // Here We are only interested in processes of which original mnt namespace belongs to zygote
708-+   // Also we just make use of existing 'q' mount pointer, no need to delcare extra mount pointer
709-+   if (is_zygote_pid) {
710-+           last_entry_mnt_id = list_first_entry(&new_ns->list, struct mount, mnt_list)->mnt_id;
711-+           list_for_each_entry(q, &new_ns->list, mnt_list) {
712-+                   if (unlikely(q->mnt_id >= DEFAULT_SUS_MNT_ID)) {
713-+                           continue;
714-+                   }
715-+                   q->mnt.susfs_mnt_id_backup = q->mnt_id;
716-+                   q->mnt_id = last_entry_mnt_id++;
717-+           }
718-+   }
719:+   // Assign the 'last_entry_mnt_id' to 'current->susfs_last_fake_mnt_id' for later use.
720-+   // should be fine here assuming zygote is forking/unsharing app in one single thread.
721-+   // Or should we put a lock here?
722:+   current->susfs_last_fake_mnt_id = last_entry_mnt_id;
723-+#endif
724-+
725-    namespace_unlock();
726-
727-    if (rootmnt)
728-@@ -3581,6 +3815,13 @@ SYSCALL_DEFINE5(move_mount,
729-    path_put(&to_path);
730- out_from:
731-    path_put(&from_path);
732-+#ifdef CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT
733-+   // For Legacy KSU mount scheme
734-+   if (!ret && susfs_is_auto_add_sus_ksu_default_mount_enabled && susfs_is_current_ksu_domain()) {
735-+           susfs_auto_add_sus_ksu_default_mount(to_pathname);
736-+   }
737-+#endif
738-+
739-    return ret;
740- }
741-
742-@@ -4037,3 +4278,37 @@ const struct proc_ns_operations mntns_operations = {
743-    .install        = mntns_install,
744-    .owner          = mntns_owner,
745- };
746-+
747-+#ifdef CONFIG_KSU_SUSFS_TRY_UMOUNT
748-+extern void susfs_try_umount_all(uid_t uid);
749-+void susfs_run_try_umount_for_current_mnt_ns(void) {
750-+   struct mount *mnt;
751-+   struct mnt_namespace *mnt_ns;
752-+
753-+   mnt_ns = current->nsproxy->mnt_ns;
754-+   // Lock the namespace
755-+   namespace_lock();
756-+   list_for_each_entry(mnt, &mnt_ns->list, mnt_list) {
757-+           // Change the sus mount to be private
--
824- #ifdef CONFIG_INOTIFY_USER
825-
826-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
827-+static void inotify_fdinfo(struct seq_file *m, struct fsnotify_mark *mark, struct file *file)
828-+#else
829- static void inotify_fdinfo(struct seq_file *m, struct fsnotify_mark *mark)
830-+#endif
831- {
832-    struct inotify_inode_mark *inode_mark;
833-    struct inode *inode;
834-@@ -84,6 +102,36 @@ static void inotify_fdinfo(struct seq_file *m, struct fsnotify_mark *mark)
835-    inode_mark = container_of(mark, struct inotify_inode_mark, fsn_mark);
836-    inode = igrab(fsnotify_conn_inode(mark->connector));
837-    if (inode) {
838-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
839:+           if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) &&
840-+                           unlikely(inode->i_state & INODE_STATE_SUS_KSTAT)) {
841-+                   struct path path;
842-+                   char *pathname = kmalloc(PAGE_SIZE, GFP_KERNEL);
843-+                   char *dpath;
844-+                   if (!pathname) {
845-+                           goto out_seq_printf;
846-+                   }
847-+                   dpath = d_path(&file->f_path, pathname, PAGE_SIZE);
848-+                   if (!dpath) {
849-+                           goto out_free_pathname;
850-+                   }
851-+                   if (kern_path(dpath, 0, &path)) {
852-+                           goto out_free_pathname;
853-+                   }
854-+                   seq_printf(m, "inotify wd:%x ino:%lx sdev:%x mask:%x ignored_mask:0 ",
855-+                                   inode_mark->wd, path.dentry->d_inode->i_ino, path.dentry->d_inode->i_sb->s_dev,
856-+                                   inotify_mark_user_mask(mark));
857-+                   show_mark_fhandle(m, path.dentry->d_inode);
858-+                   seq_putc(m, '\n');
859-+                   iput(inode);
860-+                   path_put(&path);
861-+                   kfree(pathname);
862-+                   return;
863-+out_free_pathname:
864-+                   kfree(pathname);
865-+           }
866-+out_seq_printf:
867-+#endif
868-            seq_printf(m, "inotify wd:%x ino:%lx sdev:%x mask:%x ignored_mask:0 ",
869-                       inode_mark->wd, inode->i_ino, inode->i_sb->s_dev,
870-                       inotify_mark_user_mask(mark));
871-diff --git a/fs/open.c b/fs/open.c
872-index c6646926a61a..11dfe284317b 100644
873---- a/fs/open.c
874-+++ b/fs/open.c
--
1008-   int f_flags = 0, ret = -ENOENT;
1009-   struct file *file = NULL;
1010-   struct task_struct *task;
1011-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1012-+  struct mount *mnt = NULL;
1013-+#endif
1014-
1015-   task = get_proc_task(m->private);
1016-   if (!task)
1017-@@ -54,10 +60,48 @@ static int seq_show(struct seq_file *m, void *v)
1018-   if (ret)
1019-           return ret;
1020-
1021-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1022-+  mnt = real_mount(file->f_path.mnt);
1023:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) &&
1024-+                  mnt->mnt_id >= DEFAULT_SUS_MNT_ID) {
1025-+          struct path path;
1026-+          char *pathname = kmalloc(PAGE_SIZE, GFP_KERNEL);
1027-+          char *dpath;
1028-+
1029-+          for (; mnt->mnt_id >= DEFAULT_SUS_MNT_ID; mnt = mnt->mnt_parent) { }
1030-+
1031-+          if (!pathname) {
1032-+                  goto out_seq_printf;
1033-+          }
1034-+          dpath = d_path(&file->f_path, pathname, PAGE_SIZE);
1035-+          if (!dpath) {
1036-+                  goto out_free_pathname;
1037-+          }
1038-+          if (kern_path(dpath, 0, &path)) {
1039-+                  goto out_free_pathname;
1040-+          }
1041-+          seq_printf(m, "pos:\t%lli\nflags:\t0%o\nmnt_id:\t%i\nino:\t%lu\n",
1042-+                          (long long)file->f_pos, f_flags,
1043-+                          mnt->mnt_id,
1044-+                          path.dentry->d_inode->i_ino);
1045-+          path_put(&path);
1046-+          kfree(pathname);
1047-+          goto bypass_orig_flow;
1048-+out_free_pathname:
1049-+          kfree(pathname);
1050-+  }
1051-+out_seq_printf:
1052-+  seq_printf(m, "pos:\t%lli\nflags:\t0%o\nmnt_id:\t%i\nino:\t%lu\n",
1053-+             (long long)file->f_pos, f_flags,
1054-+             mnt->mnt_id,
1055-+             file_inode(file)->i_ino);
1056-+bypass_orig_flow:
1057-+#else
1058-   seq_printf(m, "pos:\t%lli\nflags:\t0%o\nmnt_id:\t%i\nino:\t%lu\n",
--
1173-@@ -36,6 +39,9 @@
1174-   unsafe_copy_to_user(dst, src, len, label);     \
1175- } while (0)
1176-
1177-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
1178-+extern int susfs_sus_ino_for_filldir64(unsigned long ino);
1179-+#endif
1180-
1181- int iterate_dir(struct file *file, struct dir_context *ctx)
1182- {
1183-@@ -229,6 +235,11 @@ static int filldir(struct dir_context *ctx, const char *name, int namlen,
1184-           sizeof(long));
1185-   int prev_reclen;
1186-
1187-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
1188:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) && susfs_sus_ino_for_filldir64(ino)) {
1189-+          return 0;
1190-+  }
1191-+#endif
1192-   buf->error = verify_dirent_name(name, namlen);
1193-   if (unlikely(buf->error))
1194-           return buf->error;
1195-@@ -319,6 +330,11 @@ static int filldir64(struct dir_context *ctx, const char *name, int namlen,
1196-           sizeof(u64));
1197-   int prev_reclen;
1198-
1199-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
1200:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) && susfs_sus_ino_for_filldir64(ino)) {
1201-+          return 0;
1202-+  }
1203-+#endif
1204-   buf->error = verify_dirent_name(name, namlen);
1205-   if (unlikely(buf->error))
1206-           return buf->error;
1207-@@ -420,6 +436,11 @@ static int compat_fillonedir(struct dir_context *ctx, const char *name,
1208-
1209-   if (buf->result)
1210-           return -EINVAL;
1211-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
1212:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) && susfs_sus_ino_for_filldir64(ino)) {
1213-+          return 0;
1214-+  }
1215-+#endif
1216-   buf->result = verify_dirent_name(name, namlen);
1217-   if (buf->result < 0)
1218-           return buf->result;
1219-@@ -495,6 +516,11 @@ static int compat_filldir(struct dir_context *ctx, const char *name, int namlen,
1220-   buf->error = -EINVAL;   /* only used if we fail.. */
1221-   if (reclen > buf->count)
1222-           return -EINVAL;
1223-+#ifdef CONFIG_KSU_SUSFS_SUS_PATH
1224:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) && susfs_sus_ino_for_filldir64(ino)) {
1225-+          return 0;
1226-+  }
1227-+#endif
1228-   d_ino = ino;
1229-   if (sizeof(d_ino) < sizeof(ino) && d_ino != ino) {
1230-           buf->error = -EOVERFLOW;
1231-diff --git a/fs/stat.c b/fs/stat.c
1232-index 268c9eb89656..a60f22deae15 100644
1233---- a/fs/stat.c
1234-+++ b/fs/stat.c
1235-@@ -17,10 +17,17 @@
1236- #include <linux/syscalls.h>
1237- #include <linux/pagemap.h>
1238- #include <linux/compat.h>
1239-+#if defined(CONFIG_KSU_SUSFS_SUS_KSTAT) || defined(CONFIG_KSU_SUSFS_SUS_MOUNT)
1240-+#include <linux/susfs_def.h>
1241-+#endif
1242-
1243- #include <linux/uaccess.h>
1244- #include <asm/unistd.h>
1245-
1246-+#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
1247-+extern void susfs_sus_ino_for_generic_fillattr(unsigned long ino, struct kstat *stat);
1248-+#endif
1249-+
1250- /**
1251-  * generic_fillattr - Fill in the basic attributes from the inode struct
1252-  * @inode: Inode to use as the source
1253-@@ -32,6 +39,17 @@
1254-  */
1255- void generic_fillattr(struct inode *inode, struct kstat *stat)
1256- {
1257-+#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
1258:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC) &&
1259-+                  unlikely(inode->i_state & INODE_STATE_SUS_KSTAT)) {
1260-+          susfs_sus_ino_for_generic_fillattr(inode->i_ino, stat);
1261-+          stat->mode = inode->i_mode;
1262-+          stat->rdev = inode->i_rdev;
1263-+          stat->uid = inode->i_uid;
1264-+          stat->gid = inode->i_gid;
1265-+          return;
1266-+  }
1267-+#endif
1268-   stat->dev = inode->i_sb->s_dev;
1269-   stat->ino = inode->i_ino;
1270-   stat->mode = inode->i_mode;
1271-@@ -165,6 +183,12 @@ EXPORT_SYMBOL(vfs_statx_fd);
1272-  *
1273-  * 0 will be returned on success, and a -ve error code if unsuccessful.
1274-  */
1275-+
1276-+#ifdef CONFIG_KSU_SUSFS_SUS_SU
1277-+extern bool susfs_is_sus_su_hooks_enabled __read_mostly;
1278-+extern int ksu_handle_stat(int *dfd, const char __user **filename_user, int *flags);
1279-+#endif
1280-+
1281- int vfs_statx(int dfd, const char __user *filename, int flags,
1282-         struct kstat *stat, u32 request_mask)
1283- {
1284-@@ -172,6 +196,12 @@ int vfs_statx(int dfd, const char __user *filename, int flags,
1285-   int error = -EINVAL;
1286-   unsigned int lookup_flags = LOOKUP_FOLLOW | LOOKUP_AUTOMOUNT;
1287-
1288-+#ifdef CONFIG_KSU_SUSFS_SUS_SU
1289-+  if (susfs_is_sus_su_hooks_enabled) {
1290-+          ksu_handle_stat(&dfd, &filename, &flags);
1291-+  }
1292-+#endif
1293-+
--
1305-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1306-+#include <linux/susfs_def.h>
1307-+#include "mount.h"
1308-+#endif
1309- #include "internal.h"
1310-
1311- static int flags_by_mnt(int mnt_flags)
1312-@@ -84,11 +88,23 @@ EXPORT_SYMBOL(vfs_get_fsid);
1313- int vfs_statfs(const struct path *path, struct kstatfs *buf)
1314- {
1315-   int error;
1316-+#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
1317-+  struct mount *mnt;
1318-
1319-+  mnt = real_mount(path->mnt);
1320:+  if (likely(current->susfs_task_state & TASK_STRUCT_NON_ROOT_USER_APP_PROC)) {
1321-+          for (; mnt->mnt_id >= DEFAULT_SUS_MNT_ID; mnt = mnt->mnt_parent) {}
1322-+  }
1323-+  error = statfs_by_dentry(mnt->mnt.mnt_root, buf);
1324-+  if (!error)
1325-+          buf->f_flags = calculate_f_flags(&mnt->mnt);
1326-+  return error;
1327-+#else
1328-   error = statfs_by_dentry(path->dentry, buf);
1329-   if (!error)
1330-           buf->f_flags = calculate_f_flags(path->mnt);
1331-   return error;
1332-+#endif
1333- }
1334- EXPORT_SYMBOL(vfs_statfs);
1335-
1336-@@ -107,6 +123,22 @@ int user_statfs(const char __user *pathname, struct kstatfs *st)
1337-                   goto retry;
1338-           }
1339-   }
1340-+#ifdef CONFIG_KSU_SUSFS_SUS_OVERLAYFS
1341-+  /* - When mounting overlay, the f_flags are set with 'ro' and 'relatime',
1342-+   *   but this is an abnormal status, as when we inspect the output from mountinfo,
1343-+   *   we will find that all partitions set with 'ro' will have 'noatime' set as well.
1344-+   * - But what is strange here is that the vfsmnt f_flags of the lowest layer has corrent f_flags set,
1345-+   *   and still it is always changed to 'relatime' instead of 'noatime' for the final result,
1346-+   *   I can't think of any other reason to explain about this, maybe the f_flags is set by its own
1347-+   *   filesystem implementation but not the one from overlayfs.
1348-+   * - Anyway we just cannot use the retrieved f_flags from ovl_getattr() of overlayfs,
1349-+   *   we need to run one more check for user_statfs() and fd_statfs() by ourselves.
1350-+   */
1351-+  if (unlikely((st->f_flags & ST_RDONLY) && (st->f_flags & ST_RELATIME))) {
1352-+          st->f_flags &= ~ST_RELATIME;
1353-+          st->f_flags |= ST_NOATIME;
1354-+  }
1355-+#endif
--
1394-+#else
1395-   ANDROID_KABI_RESERVE(4);
1396-+#endif
1397- } __randomize_layout;
1398-
1399- struct file; /* forward dec */
1400-diff --git a/include/linux/sched.h b/include/linux/sched.h
1401-index 3b6cd312986b..1e5320dc601c 100644
1402---- a/include/linux/sched.h
1403-+++ b/include/linux/sched.h
1404-@@ -1307,8 +1307,16 @@ struct task_struct {
1405-   ANDROID_KABI_RESERVE(4);
1406-   ANDROID_KABI_RESERVE(5);
1407-   ANDROID_KABI_RESERVE(6);
1408-+#ifdef CONFIG_KSU_SUSFS
1409:+  ANDROID_KABI_USE(7, u64 susfs_task_state);
1410-+#else
1411-   ANDROID_KABI_RESERVE(7);
1412-+#endif
1413-+#ifdef CONFIG_KSU_SUSFS
1414:+  ANDROID_KABI_USE(8, u64 susfs_last_fake_mnt_id);
1415-+#else
1416-   ANDROID_KABI_RESERVE(8);
1417-+#endif
1418-
1419-   /*
1420-    * New fields for task_struct should be added above here, so that
1421-diff --git a/kernel/kallsyms.c b/kernel/kallsyms.c
1422-index 14de0c7bf756..a4c563980af4 100644
1423---- a/kernel/kallsyms.c
1424-+++ b/kernel/kallsyms.c
1425-@@ -657,8 +657,18 @@ static int s_show(struct seq_file *m, void *p)
1426-           seq_printf(m, "%px %c %s\t[%s]\n", value,
1427-                      type, iter->name, iter->module_name);
1428-   } else
1429-+#ifndef CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS
1430-           seq_printf(m, "%px %c %s\n", value,
1431-                      iter->type, iter->name);
1432-+#else
1433-+  {
1434-+          if (strstr(iter->name, "ksu_") || !strncmp(iter->name, "susfs_", 6) || !strncmp(iter->name, "ksud", 4)) {
1435-+                  return 0;
1436-+          }
1437-+          seq_printf(m, "%px %c %s\n", value,
1438-+                     iter->type, iter->name);
1439-+  }
1440-+#endif
1441-   return 0;
1442- }
1443-
1444-diff --git a/kernel/sys.c b/kernel/sys.c
1445-index fa3f48a51aa9..6f7f5236731b 100644
1446---- a/kernel/sys.c
1447-+++ b/kernel/sys.c
1448-@@ -1240,12 +1240,18 @@ static int override_release(char __user *release, size_t len)
1449-   return ret;
                                                        ========================================
9. ALL PATCH 50 TARGET FILES                            ========================================
diff --git a/fs/Makefile b/fs/Makefile
diff --git a/fs/dcache.c b/fs/dcache.c
diff --git a/fs/devpts/inode.c b/fs/devpts/inode.c
diff --git a/fs/exec.c b/fs/exec.c
diff --git a/fs/namei.c b/fs/namei.c
diff --git a/fs/namespace.c b/fs/namespace.c            diff --git a/fs/notify/fdinfo.c b/fs/notify/fdinfo.c
diff --git a/fs/open.c b/fs/open.c
diff --git a/fs/overlayfs/inode.c b/fs/overlayfs/inode.cdiff --git a/fs/overlayfs/readdir.c b/fs/overlayfs/readdir.c
diff --git a/fs/overlayfs/super.c b/fs/overlayfs/super.c
diff --git a/fs/proc/bootconfig.c b/fs/proc/bootconfig.c
diff --git a/fs/proc/fd.c b/fs/proc/fd.c                diff --git a/fs/proc/task_mmu.c b/fs/proc/task_mmu.c    diff --git a/fs/proc_namespace.c b/fs/proc_namespace.c
diff --git a/fs/readdir.c b/fs/readdir.c                diff --git a/fs/stat.c b/fs/stat.c                      diff --git a/fs/statfs.c b/fs/statfs.c                  diff --git a/include/linux/mount.h b/include/linux/mount.h                                                      diff --git a/include/linux/sched.h b/include/linux/sched.h
diff --git a/kernel/kallsyms.c b/kernel/kallsyms.c
diff --git a/kernel/sys.c b/kernel/sys.c
root@localhost:~/oscar#
